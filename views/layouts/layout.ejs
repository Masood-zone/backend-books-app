<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/styles/output.css" rel="stylesheet" />
    <title>KiritoLibrary</title>
  </head>
  <!-- Converted base styling to TailwindCSS -->
  <body class="bg-gray-100">
    <div class="container mx-auto">
      <!-- Header -->
      <%- include('../partials/header.ejs',{ currentPage: locals.currentPage })
      %>
      <!-- Error messages -->
      <%- include('../partials/errorMessage.ejs', { errorMessage:
      locals.errorMessage }) %>
      <!-- Body -->
      <%- body %>
    </div>
  </body>
</html>
